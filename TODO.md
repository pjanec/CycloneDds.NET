[TODO] Upgrade to cyclone dds 11.0.

[BUG] DdsMonitor: too strict component type  def
in ddsmon-settings.json "ComponentTypeName" contains full assembly id including version like

    "ComponentTypeName": "DdsMonitor.Components.TopicExplorerPanel, DdsMonitor, Version=0.2.0.0, Culture=neutral, PublicKeyToken=null",

It should be just the full namespace & class, exclusding the version and culture etc. like:

    "ComponentTypeName": "DdsMonitor.Components.TopicExplorerPanel",

[IMPROVEMENT] DdsMonitor: sorted topic in topic sources
In the Topic Sources panel
    the table "Topics in selected assembly" 
      - should be alphabetically sorted by topic name
      - CLR type should show the namespace as the recond row (grayed a bit)


[IMPROVEMENT] DdsMonitor: full folder scan in topic sources
In the Topic Sources panel
  - the path could be a folder path (no concrete dll name)
     - system will scan all potential assemblies (*.dll and *.exe etc) automatically
     - the CLR type in "Topics in selected assembly" will show 3-lines (1. base type name, 2. namespace, 3. assembly file path)

  - the scan will happen every time the dds monitor is started, auto-adapting to possible dll changes
    since the last run


[IMPROVEMENT] More useful schema compiler info message
schema compiler now writes "Running CycloneDDS Code Generator (Incremental)..." many times when compiling a larger project.
I need it to bring more information, like what part of the project is just being compiled

[IMPROVEMENT] dds monitor shows unfriendly source time stamp in "Sample info" tab of sample detail
Now it looks like

    "SourceTimestamp: 1773826775412144300"

I need it in standard format in LOCAL time zone, similar to 

    "2026-03-18 09:39:35.4147021"

to match the display of incoming time stamp


The Timestamp shown in the All Samples window is probably showing UTC time and not local time zone.
I need it to show LOCAL time zone.

[BUG] union list item shown in sample detail's "Table" tab can't be exanded to see the union content
if the sample contains a list of uniton, the list shows just the union type names as its items (OK)
but does not allow expanding them to see their inner content (discriminator and the content of corresponding union arm).
Only the active union arm needs to be shown, similarly as the union is displayed in the "Tree" tab.

[IMPROVEMENT] The union field is now displayed as the union type name string which can be expanded to
show the discriminator and the active union arm.
Instead of the uniton type name (which is useless, and inconsistent as we do not show types for other fields)
we cuold show the union discriminator field directly.
And when expanded, only the content of the active union arsm should be shown.



[IMPROVEMENTS] The tree tab of sample detail panel should have icons for "collapse all" and "expand all"


[BUG] in sample details, null string is shown as empty string - unrecognizable from it
null string should be should in a special way, different from usual string - maybe as "null" but in different color
than usual strings are so we know it is not a 4-character string but actually a null value.

[IMPROVEMENT] dds mon enum field values should be shown in different color than numeric field values and different
than string values and different for struct headers to the field data type is recignizable at first glance
and not accidentally visually mistaken with string representation for example



[IMPROVEMENT] ddsmon should support alternative formatting of fields
For example if DIS type is represented as plain long, the DDS monitor, in sample detail, should allow on right click
to open context menu offering "display as DIS type".

These alternative formatter should be registerable dynamically. Each should support a function taking the sample
information and the individual field information and returning whether this alternative renderer should be offered
in the context menu for such a field (inside it can apply some hauristir checking the field name, sample type,
sample fields etc. to guess whether it is applicable).

maybe the formatter could return a float 0..1 indicating how much 'suitable' it is for given field, enabling sorting
in the context menu.

If this value is high enough, it might be auto-applied by default (while still keeping the context menu to allow switching to
default renderer of other alternative renderers)

The alternative formatters should be applicable on whole structures, to show the 'preview' the structure content
directly in the field line instead uf useless structure type name shown now.
For example, Vec3-type alternative renderer might show [12.567, 52.5472, -1836.356] instead of useless "BasicData.Vec3"
These autogenerated preview should be syntax colored to allow for easy reading (square brackets with more muted color than numbers etc). 


[BUG] In all samples panel, when trying to sort the samples by clicking on the table column "Ordinal" header,
the visual sort indicator changes (arrow up/arrow down) but the samples are always shown in the ascending ordinal
order. Showing the last incoming samples first is a crucial feature when monitoring so this should be well
supported - it must work in the first place, ans in must be performant (not causing to send all samples
from server to client every time a new sample arrives...)

[IMPROVEMENTS] Samples autoscroll
Also, the All Samples should support autoscroll capability if ascending ordinal order is active.
If the selection is on the currently last sample and a new sample arrives, the selection
should AUTOMATICALLY change to the most recently receiveid sample, this row should be made visible
on the screen (by scrolling the sample list) and if there is a linked sample detail window,
it should be updated to reflect the newly selected sample.


[BUG] the red Reset button in main menu deletes all received samples but it also unsubscribes all so i have to
click 'Subscribe all' checkbox in the topic. Reset shoudl not forget the active subscriptions!

[BUG] In Samples panel the Timestamp field is now shown by default and can not be removed.
I would like to make it selectable from the column list as any other rows.
If i select now the Timestamp from the column list (it is there already), a new column is added
to the table but is always empty!
Similarly to the filter, the column available for selection could include also non-payload fields.
The only fixed columns always shown could be be just the Ordinal which is necessary as a UI for
reversing the sort (latest on the top vs latest at the bottom)



[IMPROVEMENT] In the All Smaples window I need to be able to filter OUT some message types (usually the
most frequent ones flooding the sample panel).
1. this must be doable via the existing filter mechanism, by adding condition for topic type name
     - in the filter panel i do not see a way how to filter by something else but the payload fields
     - i am lacking some way of selecting the non-payload fields like topic name, sample state etc.
     - maybe we can prepend the existing field list items with "Payoad." prefix and add also other list
        where the non-payload fields can be selected and used in the filter
2. there should be a right click context menu on the sample allowing to EXTEND already existing filter
   by removing the clicked message type.


[IMPROVEMENT]
On topics panel, when right clicking on a topic type in the topic table, I want a new "topic properties"
window to be opened, showing
 - the actual topic name used by the DDS backend
 - topic fields in a flat list table (alpha sorted by field name) with columns
    1. field name (MyStruct.SubStrict.Field1)
    2. data type (full namespace)
    3. isKeyField flag

 - byte size of the plain-c data structure
 - ?? maybe some other topic properties if available

The window is non-modal. It should use standard window index for saving and resoting the state
(so if i open the window next time, it shows on the same position) - state persistence should be window index based,
not topci type based.

It shoud use modern styling (no densely placed ugly controls).





[IMPROVEMENT]
The current selection of visible columns in samples panels should be saved and restored with the panel info.
This is important especially for the sample-type specific sample windows.
There should be an easy way to reset this to defaults - via some button on the column selection window.
